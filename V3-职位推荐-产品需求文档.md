# AI 简历编辑器 v3：职位推荐产品需求文档

## 1. 文档信息

- 产品：AI 简历编辑器
- 版本：v3
- 模块：职位推荐
- 状态：需求确认稿
- 日期：2026-06-08
- 产品定位：基于已收集职位的职位管理、面试进度记录、简历匹配分析与简历改写

## 2. 背景与目标

现有产品已经具备简历编辑、预览、保存和导出能力。用户在求职过程中还需要管理已收集的岗位、记录投递与面试进度，并针对具体 JD 判断简历匹配度。

v3 在第二个 tab 增加“职位推荐”。用户进入页面后直接看到由飞书表格同步的职位数据，不需要在产品内导入岗位，也不需要登录第三方招聘平台。

核心闭环：

1. 查看已收集职位。
2. 筛选职位并查看详情。
3. 记录投递和面试进度。
4. 点击按钮发起 AI 匹配度分析。
5. 在右侧交互区查看分析结果和建议。
6. 点击按钮发起 AI 简历改写。
7. 查看改写建议，为后续应用到简历编辑器预留能力。

### 2.1 产品目标

- 集中管理用户已经收集的岗位。
- 减少岗位、简历和面试记录之间的切换成本。
- 基于岗位要求和现有简历给出可解释的匹配分析。
- 基于真实简历内容给出针对具体岗位的改写建议。
- 为后续接入 AI 模型 API 建立稳定的输入、提示词和输出协议。

### 2.2 成功指标

- 职位列表访问人数与人均查看职位数。
- 职位详情打开率。
- 面试进度记录率。
- 匹配度分析按钮点击率、成功率与平均耗时。
- 简历改写按钮点击率、成功率。
- 分析结果查看完成率。
- 改写建议采纳率，待支持采纳功能后统计。

## 3. 产品范围

### 3.1 v3 一期

- “职位推荐”位于左侧导航第二个 tab。
- 职位数据从既有飞书表格同步。
- 用户进入页面后直接看到职位列表。
- 支持搜索、筛选、排序和查看职位详情。
- 支持记录求职和面试进度。
- 预留“匹配度分析”按钮。
- 点击后调用 AI 模型 API，在右侧交互区展示结果。
- 预留“简历改写”按钮。
- 点击后调用 AI 模型 API，按规则输出改写建议。
- 保存职位进度、分析结果和改写结果。

### 3.2 明确不做

- 用户在产品内导入岗位链接或岗位正文。
- 登录第三方招聘平台。
- 抓取或批量采集第三方招聘平台。
- 自动投递。
- 自动与招聘方沟通。
- 全网职位搜索和主动推荐。
- 薪资真实性判断和公司背调。

> “职位推荐”一期实际是职位管理和职位适配分析。岗位推荐算法不在本期范围。

## 4. 信息架构

### 4.1 导航

1. 简历编辑
2. 职位推荐
3. 简历模板
4. 模拟面试
5. AI 导师

### 4.2 页面布局

桌面端采用左右分栏：

- 左侧主区域：职位列表或职位详情。
- 右侧交互区：当前职位的 AI 分析、改写结果和追问入口。

建议比例为 `65% : 35%`。两个区域独立滚动，右侧始终显示当前引用职位。

### 4.3 页面层级

- 职位列表
- 职位详情
- 匹配度分析结果
- 简历改写结果
- 面试进度编辑

## 5. 数据来源与同步

### 5.1 数据来源

一期职位数据来自此前使用的飞书表格。职位收集和维护继续在飞书侧完成，本产品负责读取、展示和使用这些数据。

用户核心流程中不出现“导入职位”。首次进入页面时：

- 已有数据：直接展示职位列表。
- 正在同步：展示列表骨架屏和同步状态。
- 暂无数据：提示“暂无职位数据，请先在飞书表格维护职位”。
- 同步失败：展示最近一次成功同步的数据，并提示刷新。

### 5.2 同步方式

具体实现可根据现有飞书表格类型选择：

- 飞书电子表格：通过服务端任务读取指定工作表。
- 飞书多维表格：通过服务端任务读取指定数据表记录。
- 一期也可先将飞书数据导出为固定 JSON，由产品读取；前端数据协议保持不变。

同步建议：

- 页面打开时拉取一次。
- 提供“刷新职位”按钮。
- 服务端可配置定时同步。
- 使用飞书记录 ID 作为外部唯一标识。
- 数据更新以飞书岗位信息为准。
- 产品内维护的求职进度、AI 结果不得被飞书同步覆盖。

### 5.3 数据分层

**飞书主数据**

- 岗位名称、公司、地区、业务方向、JD 等职位信息。
- 只读展示，修改应回到飞书完成。

**产品侧用户数据**

- 收藏、投递和面试进度。
- 用户备注。
- 匹配度分析结果。
- 简历改写结果。
- 分析所使用的简历快照。

### 5.4 同步规则

- 新飞书记录：新增职位。
- 已存在记录：更新职位主数据。
- 飞书删除记录：产品标记为“来源已删除”，不立即删除用户进度和 AI 结果。
- 字段为空：保留空值，不用模型猜测。
- 重复数据：优先按飞书记录 ID 去重。
- 同步时间写入 `lastSyncedAt`。

## 6. 职位字段定义

飞书表格应尽量提供以下字段。字段名可通过映射配置适配现有表头。

### 6.1 核心字段

| 标准字段 | 飞书字段示例 | 必填 | 列表展示 | 用途 |
| --- | --- | --- | --- | --- |
| `externalRecordId` | 记录 ID | 是 | 否 | 同步与去重 |
| `jobTitle` | 岗位名称 | 是 | 是 | 职位识别 |
| `companyName` | 公司名称 | 是 | 是 | 职位识别 |
| `location` | 所在地区 | 否 | 是 | 展示与筛选 |
| `businessDirection` | 业务方向 | 否 | 是 | 展示与匹配 |
| `jobDescription` | 岗位详情 / JD | 是 | 摘要 | AI 分析主要输入 |
| `sourceUrl` | 职位链接 | 否 | 否 | 打开原岗位 |

### 6.2 匹配分析字段

以下字段可以由飞书直接维护，也可以在同步后通过确定性规则从 JD 拆分；一期不要求单独调用模型预处理。

| 标准字段 | 说明 |
| --- | --- |
| `responsibilities` | 工作职责 |
| `requirements` | 任职要求 |
| `skills` | 技能、工具和领域关键词 |
| `experienceRequired` | 工作年限要求 |
| `educationRequired` | 学历要求 |
| `majorRequired` | 专业要求 |
| `industry` | 所属行业 |
| `seniority` | 职级 |
| `languageRequirements` | 语言要求 |

### 6.3 辅助字段

| 标准字段 | 说明 |
| --- | --- |
| `salaryText` | 原始薪资文本 |
| `employmentType` | 全职、兼职或实习 |
| `companySize` | 公司规模 |
| `financingStage` | 融资或上市状态 |
| `benefits` | 福利待遇 |
| `workAddress` | 详细工作地点 |
| `recruiterName` | 招聘联系人 |
| `publishedAt` | 发布时间 |
| `sourcePlatform` | 来源平台 |
| `collectedAt` | 收集时间 |

### 6.4 产品侧字段

| 字段 | 说明 |
| --- | --- |
| `id` | 产品内职位 ID |
| `jobStatus` | 求职进度 |
| `statusUpdatedAt` | 进度更新时间 |
| `nextActionAt` | 下一步时间 |
| `userNotes` | 用户备注 |
| `lastSyncedAt` | 最近同步时间 |
| `sourceDeleted` | 飞书来源是否已删除 |
| `matchAnalysis` | 最近一次匹配分析 |
| `rewriteResult` | 最近一次简历改写结果 |
| `resumeSnapshotId` | AI 任务使用的简历快照 |

## 7. 职位列表区

### 7.1 列表卡片

必须展示：

- 岗位名称
- 公司名称
- 所在地区
- 业务方向
- 当前求职进度
- 匹配分析状态

有数据时展示：

- 薪资
- 经验要求
- 学历要求
- 来源平台
- 收集时间
- 下一步面试时间

### 7.2 列表操作

- 查看详情
- 匹配度分析
- 简历改写
- 更新求职进度
- 打开原职位链接
- 添加备注

一期不提供删除飞书职位的入口。可提供“隐藏职位”，仅影响当前产品展示。

### 7.3 搜索、筛选与排序

搜索：

- 岗位名称
- 公司名称
- 业务方向

筛选：

- 地区
- 业务方向
- 求职进度
- 是否已分析

排序：

- 收集时间
- 最近更新
- 匹配度
- 下一步面试时间

### 7.4 求职进度

建议状态：

- 待评估
- 准备投递
- 已投递
- 简历筛选
- 一面
- 二面
- 终面
- Offer
- 已拒绝
- 已放弃

更新进度时可同时填写：

- 进度发生时间
- 下一步安排时间
- 面试轮次
- 备注

## 8. 职位详情页

详情页包含：

1. 岗位名称、公司、地区、薪资。
2. 业务方向、行业、经验和学历标签。
3. 当前求职进度及更新时间。
4. 匹配度分析摘要，未分析时显示按钮。
5. 工作职责。
6. 任职要求。
7. 福利待遇与工作地址。
8. 用户备注与下一步安排。
9. 来源平台、原始链接和同步时间。

固定操作：

- 匹配度分析 / 重新分析
- 简历改写 / 重新生成
- 更新进度
- 打开原职位

## 9. 右侧交互区

### 9.1 初始状态

用户仅浏览职位时，交互区展示：

- 当前引用职位。
- 当前使用的简历名称。
- “匹配度分析”按钮。
- “简历改写”按钮。
- 已有历史结果入口。

不会在用户进入页面后自动调用模型，避免无意消耗 API。

### 9.2 运行状态

点击按钮后：

1. 校验职位 JD 与当前简历是否完整。
2. 创建当前简历快照。
3. 组装对应任务的模型输入。
4. 展示生成中状态。
5. 模型返回后校验结构化结果。
6. 在交互区渲染结果并持久化。

用户切换职位后，交互区同步切换到该职位的上下文和历史结果。

### 9.3 通用交互

- 展示“引用职位：公司 / 岗位”。
- 展示“基于简历：简历名称 / 快照时间”。
- 支持重新生成。
- 支持复制结果。
- 预留继续追问输入框。
- 简历或 JD 更新后，旧结果标记为“基于历史版本”。

## 10. AI 模型接入总策略

### 10.1 任务拆分

使用两个独立模型任务和两套提示词：

1. `job_match_analysis`：匹配度分析。
2. `resume_rewrite`：简历改写。

不得使用一个模糊的通用对话提示词同时承担评分和改写。

### 10.2 输入处理

发送模型前：

- 移除手机号、邮箱、头像等无关个人敏感信息。
- 将简历 HTML 转换为结构化纯文本或 JSON。
- 保留公司、岗位、项目、职责、时间和量化结果之间的对应关系。
- 保留完整 JD，并同时传入已拆分的职责和要求字段。
- 写入稳定的字段 ID，方便模型结果定位到简历模块。
- 记录 `jobVersion`、`resumeSnapshotId` 和 `promptVersion`。

### 10.3 输出约束

- API 应要求模型返回 JSON，不直接依赖自由文本。
- 服务端使用 JSON Schema 校验结果。
- 校验失败时允许携带错误信息重试一次。
- 展示层根据 JSON 渲染，不解析 Markdown 标题猜测结构。
- 每条结论必须尽量包含 JD 依据与简历依据。
- 模型不得补充输入中不存在的事实或数字。

### 10.4 模型参数建议

- 匹配分析使用低随机性，建议 `temperature: 0.1–0.3`。
- 简历改写允许适度表达变化，建议 `temperature: 0.3–0.5`。
- 使用支持结构化输出的模型。
- 为两个任务分别设置超时、重试和 token 上限。
- 相同职位版本、简历快照和提示词版本可命中缓存。

### 10.5 失败处理

- 超时或限流：保留任务上下文，允许重试。
- JSON 校验失败：自动纠正重试一次。
- 输入过长：优先保留 JD、个人优势和相关项目，提示结果可能不完整。
- 简历缺少内容：不直接生成高分或虚构内容，返回“信息不足”。
- 模型不可用：职位管理与面试进度仍可正常使用。

## 11. 匹配度分析

### 11.1 触发方式

- 职位卡片“匹配度分析”按钮。
- 职位详情页“匹配度分析”按钮。
- 右侧交互区快捷按钮。

点击后才调用模型。生成中按钮禁用，并展示进度状态。

### 11.2 评分维度

总分 100 分：

| 维度 | 权重 | 内容 |
| --- | ---: | --- |
| 核心职责匹配 | 25% | 是否做过相似工作、承担相近职责 |
| 技能与关键词 | 20% | 工具、方法、专业技能和领域能力 |
| 项目与成果 | 20% | 是否有相关项目与量化结果 |
| 工作年限与职级 | 15% | 总年限、相关年限、管理范围 |
| 行业与业务方向 | 10% | 行业、业务模式、产品阶段 |
| 学历与基础条件 | 5% | 学历、专业和语言等要求 |
| 地区与求职偏好 | 5% | 地区和目标岗位方向 |

硬性要求单独判断。若不满足明确的学历、年限、语言等硬性条件，应显示风险，但不能简单地仅靠一个条件将总分归零。

### 11.3 输出结构

```json
{
  "overallScore": 78,
  "confidence": "high",
  "conclusion": "值得优先投递",
  "summary": "一句话结论",
  "dimensions": [
    {
      "key": "core_responsibilities",
      "name": "核心职责匹配",
      "weight": 25,
      "score": 21,
      "status": "matched",
      "jobEvidence": ["JD 中的要求"],
      "resumeEvidence": ["简历中的对应经历"],
      "reason": "评分解释"
    }
  ],
  "strengths": [],
  "gaps": [],
  "hardRequirementRisks": [],
  "missingEvidence": [],
  "resumeSuggestions": [],
  "keywords": [],
  "disclaimer": "匹配度仅供求职决策参考"
}
```

### 11.4 分析策略

- 先识别 JD 的核心职责、加分项和硬性要求。
- 再从简历中寻找明确证据。
- 按维度评分后求和，不允许模型直接凭整体印象给总分。
- 每个维度分数不得超过对应权重。
- 总分必须等于维度得分之和。
- 区分“不匹配”和“简历未体现”。
- 无证据时不得推断用户具备该能力。
- 关键词仅作为辅助，不按出现次数机械评分。
- 输出分析置信度：`high`、`medium`、`low`。

### 11.5 匹配分析系统提示词

```text
你是严谨的求职匹配分析器。你的任务是比较职位信息与候选人简历，
输出可解释、可校验的结构化匹配结果。

规则：
1. 只能使用输入中的职位和简历事实，不得补充或猜测经历、技能、学历、年限和数据。
2. “简历未体现”不等于“候选人不具备”，必须单独标记为 missing_evidence。
3. 先提取职位的核心职责、硬性要求和加分项，再寻找简历证据。
4. 按给定权重逐维度评分。每个维度得分不得超过权重，总分必须等于维度得分之和。
5. 每项重要判断需要给出 jobEvidence 和 resumeEvidence；没有证据时数组为空。
6. 不得仅按关键词出现次数评分。
7. 明确指出硬性条件风险，但不要承诺面试或录用结果。
8. 只返回符合指定 JSON Schema 的 JSON，不要输出 Markdown 或额外说明。
```

### 11.6 匹配分析用户提示词模板

```text
请分析以下职位与简历的匹配度。

【评分权重】
核心职责 25；技能关键词 20；项目成果 20；年限职级 15；
行业业务 10；学历基础条件 5；地区求职偏好 5。

【职位数据】
{{job_json}}

【简历数据】
{{resume_json}}

【用户求职偏好】
{{preference_json}}

请严格按照 JSON Schema 输出，并校验：
- overallScore 等于所有 dimensions.score 之和；
- 每个维度得分不超过该维度 weight；
- 所有事实均可在输入中找到依据。
```

## 12. 简历改写

### 12.1 触发方式

- 职位卡片“简历改写”按钮。
- 职位详情页“简历改写”按钮。
- 匹配分析结果中的“基于缺口优化简历”按钮。

如果尚未执行匹配分析，也允许直接改写；系统在内部先提取 JD 重点，但不额外展示匹配分。

### 12.2 一期输出范围

一期先在交互区展示改写建议，预留后续“应用到简历”能力。

可建议改写：

- 意向岗位
- 个人关键词
- 个人优势
- 项目职责
- 项目总结
- 重点项目描述
- 内容排序和删减建议

不得改写：

- 姓名、手机号、邮箱。
- 公司名称、学校名称。
- 任职和教育时间。
- 输入中不存在的项目、技能、数据和奖项。

### 12.3 输出结构

```json
{
  "strategySummary": "本次改写策略",
  "targetKeywords": [],
  "suggestions": [
    {
      "sectionType": "project_summary",
      "sectionId": "project_id",
      "originalText": "原文",
      "rewrittenText": "建议稿",
      "reason": "修改原因",
      "jobEvidence": ["对应 JD 要求"],
      "factsUsed": ["使用的原简历事实"],
      "riskLevel": "low",
      "needsUserInput": false,
      "question": null
    }
  ],
  "missingInformation": [],
  "doNotChange": [],
  "disclaimer": "请在确认事实准确后使用改写内容"
}
```

### 12.4 改写策略

- 优先调整表达和排序，不改变事实。
- 使用 JD 中的专业表达，但避免关键词堆砌。
- 将与岗位相关的项目和成果前置。
- 优先采用“行动 + 方法 + 结果”的表达。
- 只能复用简历已有量化数据。
- 缺少结果数据时提示用户补充，不生成虚假数字。
- 每条建议绑定原简历字段 ID，便于后续逐项应用。
- `factsUsed` 必须列明改写使用了哪些原始事实。
- 存在事实扩张风险时标记 `riskLevel: high`，不得自动应用。

### 12.5 简历改写系统提示词

```text
你是专业且保守的中文简历改写助手。你的任务是根据目标职位优化简历表达，
但绝不能创造候选人没有提供的事实。

规则：
1. 只能使用输入简历中已经存在的公司、项目、职责、技能、数据和奖项。
2. 不得编造数字，不得扩大候选人的职责范围，不得把参与描述成主导。
3. 可以调整措辞、结构、顺序和篇幅，使真实经历更贴近职位要求。
4. 优先使用“行动 + 方法 + 结果”的表达，但缺少结果时应提出补充问题。
5. 可以使用 JD 中的专业关键词，但只有与简历事实一致时才能写入建议稿。
6. 每条建议必须保留 sectionId，并返回 originalText、rewrittenText、reason、
   jobEvidence、factsUsed、riskLevel 和 needsUserInput。
7. 联系方式、公司、学校和时间字段不得修改。
8. 只返回符合指定 JSON Schema 的 JSON，不要输出 Markdown 或额外说明。
```

### 12.6 简历改写用户提示词模板

```text
请针对目标职位生成中文简历改写建议。

【目标职位】
{{job_json}}

【当前简历】
{{resume_json}}

【可改写字段】
{{editable_sections_json}}

【已有匹配分析，可为空】
{{match_analysis_json}}

要求：
- 优先强化与核心职责和硬性要求相关的真实经历；
- 不得引入输入中不存在的事实和数字；
- 缺少关键信息时写入 missingInformation，不要自行补全；
- 每条建议必须绑定原 sectionId；
- 只返回符合 JSON Schema 的 JSON。
```

## 13. AI 结果展示

### 13.1 匹配度分析

交互区按以下顺序展示：

1. 总匹配度、结论和置信度。
2. 各维度得分。
3. 已匹配优势。
4. 关键缺口。
5. 硬性条件风险。
6. 简历未体现的信息。
7. 简历优化建议。

### 13.2 简历改写

按简历模块展示：

- 原文
- 建议稿
- 修改原因
- 对应 JD 依据
- 使用的简历事实
- 风险提示

一期按钮：

- 复制建议稿
- 重新生成
- 继续追问，预留
- 应用到简历，预留且默认禁用

## 14. 数据状态

### 14.1 求职进度

`not_evaluated`、`preparing`、`applied`、`screening`、`interview_1`、
`interview_2`、`final_interview`、`offer`、`rejected`、`withdrawn`

### 14.2 AI 任务状态

`idle`、`queued`、`running`、`succeeded`、`failed`、`outdated`

### 14.3 结果失效

以下情况将历史 AI 结果标记为 `outdated`：

- 飞书同步后 JD 内容变化。
- 当前简历发生变化。
- 用户切换简历。
- 提示词版本或评分规则升级。

历史结果仍可查看，但重新分析时使用最新数据。

## 15. 异常处理

| 场景 | 处理 |
| --- | --- |
| 飞书同步失败 | 展示缓存数据和最近同步时间，允许刷新 |
| 飞书字段缺失 | 展示已有字段，缺少 JD 时禁用 AI 按钮 |
| 职位来源已删除 | 保留用户进度和历史结果，标记来源已删除 |
| 当前简历内容不足 | 提示先完善简历，或以低置信度分析 |
| 模型请求超时 | 保留上下文并提供重试 |
| 模型限流 | 提示稍后重试，不影响职位管理 |
| 模型 JSON 不合法 | 自动纠正重试一次，仍失败则展示错误 |
| 模型结果无证据 | 标记结果异常，不展示虚假高分 |
| 简历或 JD 已更新 | 历史结果标记过期 |

## 16. 隐私与安全

- 飞书访问凭证保存在服务端，不下发到浏览器。
- AI 请求默认移除手机号、邮箱和头像。
- 明确告知用户职位与简历内容会发送给模型服务。
- 日志不记录完整简历、完整 JD 或联系方式。
- 保存模型名称、提示词版本和任务状态，不保存模型密钥。
- 用户可删除 AI 分析结果和改写结果。

## 17. 埋点

- `jobs_page_viewed`
- `jobs_sync_started`
- `jobs_sync_succeeded`
- `jobs_sync_failed`
- `job_detail_viewed`
- `job_status_updated`
- `job_note_updated`
- `match_analysis_started`
- `match_analysis_succeeded`
- `match_analysis_failed`
- `match_analysis_retried`
- `resume_rewrite_started`
- `resume_rewrite_succeeded`
- `resume_rewrite_failed`
- `rewrite_result_copied`

埋点不得包含职位正文、简历正文或联系方式。

## 18. MVP 验收标准

### 18.1 职位数据

- 页面进入后直接展示飞书职位数据。
- 用户流程中不存在职位导入。
- 能按飞书记录 ID 增量同步和去重。
- 飞书同步不得覆盖产品侧求职进度和 AI 结果。
- 同步失败时可继续查看最近数据。

### 18.2 职位管理

- 列表展示岗位名称、公司、地区、业务方向和进度。
- 支持搜索、筛选和排序。
- 支持查看完整职位详情。
- 支持更新投递及面试进度、时间和备注。

### 18.3 匹配度分析

- 仅在用户点击按钮后调用模型。
- 结果展示在当前职位对应的右侧交互区。
- 结果包含总分、维度分、优势、缺口、风险和建议。
- 总分等于各维度得分之和。
- 重要结论能追溯到 JD 与简历证据。
- 无证据时不能推断用户具备相关能力。
- 简历或 JD 更新后旧结果被标记为过期。

### 18.4 简历改写

- 仅在用户点击按钮后调用模型。
- 结果按原文、建议稿、原因和事实依据展示。
- 不修改联系方式、公司、学校和时间等事实字段。
- 不引入简历中不存在的经历、技能、数字和奖项。
- 一期支持复制结果，并预留应用到简历的按钮和数据结构。

## 19. 待技术确认

1. 现有数据源是飞书电子表格还是飞书多维表格。
2. 现有飞书表头与本文标准字段的映射关系。
3. 同步由前端、服务端接口还是定时任务执行。
4. AI 模型供应商、具体模型和结构化输出能力。
5. 模型 API 是否支持 JSON Schema。
6. 一期职位进度存储在本地还是服务端。
7. 继续追问是否纳入一期，还是仅展示固定分析结果。

## 20. 后续版本

- 将 AI 建议逐项应用到简历编辑器。
- 为不同职位创建独立简历版本。
- 面试日程提醒。
- 将 JD 和分析结果带入模拟面试。
- 将能力缺口同步到 AI 导师。
- 多职位横向对比。
- 基于职位池和简历画像做真正的岗位推荐。
