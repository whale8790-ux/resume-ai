# AI 简历编辑器功能更新归档

## 文档信息

- 项目：AI 简历编辑器
- 更新范围：第一阶段、第二阶段
- 归档日期：2026-06-08
- 使用场景：个人在本地浏览器中编辑、保存和导出简历
- 当前数据版本：`schemaVersion: 1`

## 更新背景

本轮更新优先解决两类核心风险：

1. 简历源数据仅保存在浏览器中，存在丢失、无法迁移和旧数据导致页面异常的风险。
2. PDF 导出前缺少质量检查，超长内容可能造成分页异常或页面裁切。

更新原则是先保障内容安全和导出可靠性，再进行工程化及体验优化。

---

## 第一阶段：数据安全与恢复

### 1. JSON 数据备份

顶部工具栏新增“数据”入口，支持将当前简历导出为 JSON 备份。

备份文件包含：

- 数据结构版本号
- 导出时间
- 简历完整源数据
- 头像数据
- 个人信息、工作经历、项目经历和教育经历

JSON 是可继续编辑的源数据备份，与只能查看的 PDF 用途不同。

### 2. JSON 数据导入

支持从本地选择 JSON 文件并恢复简历。

导入规则：

- 兼容本项目导出的标准备份格式。
- 兼容不带外层包装的旧版简历数据。
- 导入前进行数据格式识别和结构校验。
- 不属于简历数据的普通 JSON 文件会被拒绝。
- 高于当前数据版本的备份会被拒绝，避免旧程序错误处理新格式。
- 导入前需要二次确认。

### 3. 数据版本与旧数据兼容

简历数据增加 `schemaVersion` 字段，当前版本为 `1`。

加载缓存、导入备份和恢复历史时会统一执行数据标准化：

- 补齐缺失的个人信息字段。
- 补齐工作经历、项目经历和教育经历数组。
- 保证至少存在一条工作经历、项目经历和教育经历。
- 为缺失的数据对象补充唯一 ID。
- 修复字段类型异常。
- 处理刷新后失效的 `blob:` 头像地址。
- 忽略无法识别的冗余字段。

这可以避免旧缓存或不完整数据导致页面白屏。

### 4. 本地历史快照

每次自动保存时同步生成历史快照。

快照规则：

- 最多保留最近 10 份。
- 内容没有变化时不重复保存。
- 浏览器存储空间不足时优先淘汰较旧快照。
- 可在“数据管理”中查看快照时间和简历名称。
- 恢复历史快照前需要二次确认。

### 5. 覆盖前保护

以下操作执行前，会先将当前内容立即保存为历史快照：

- 导入 JSON 备份
- 恢复历史快照
- 恢复默认数据

即使用户刚完成编辑、600ms 自动保存尚未触发，也可以从历史中找回覆盖前的内容。

### 6. 恢复默认数据

“数据管理”中新增“恢复默认示例数据”。

行为规则：

- 操作前二次确认。
- 当前内容先保存为历史快照。
- 恢复后继续参与自动保存。
- 可通过历史快照撤回本次恢复。

---

## 第二阶段：导出质量与分页可靠性

### 1. 明确的保存状态

顶部状态区域由单一更新时间升级为三种实时状态：

- `保存中…`：数据已修改，正在等待或执行自动保存。
- `已保存`：本地保存成功，并显示最后更新时间。
- `保存失败`：浏览器存储写入失败。

保存失败时会持续显示错误状态，并提示优先导出 JSON 备份。

### 2. 导出前完整度检查

点击“下载”后，不再立即生成 PDF，而是先执行简历质量检查。

检查结果分为：

- 必须修复：阻止导出。
- 优化建议：允许用户确认后继续导出。

### 3. 必须修复项

以下情况会阻止 PDF 导出：

- 姓名为空。
- 公司名称为空。
- 任意预览页内容超出 A4 页面边界。

用户需要返回编辑器修复后再导出。

### 4. 优化建议项

以下情况会提示，但允许继续导出：

- 手机号为空或号码长度异常。
- 邮箱为空或格式异常。
- 意向岗位为空。
- 个人优势为空。
- 项目名称为空。
- 项目职责为空。
- 项目内容为空。
- 学校名称为空。
- 日期格式不是 `YYYY-MM`。
- 开始时间晚于结束时间。
- 单个项目内容过长，影响简历阅读效率。

检查范围覆盖公司任职时间、项目时间和教育时间。

### 5. 页面溢出检查

导出前逐页比较页面的实际内容尺寸和 A4 容器尺寸。

如果出现垂直或水平溢出：

- 在导出检查中标出具体页码。
- 阻止导出。
- 提示精简超长内容。

### 6. 超长内容自动拆分

分页逻辑增强，以下内容会被拆成可独立分页的小块：

- 个人优势
- 项目总结
- 重点项目
- 主要荣誉
- 无序列表和有序列表中的单条长内容

拆分规则：

- 每块目标上限约 700 字。
- 优先在换行、句号、分号、感叹号、问号和逗号处拆分。
- 正常长度内容保留原始富文本格式。
- 异常超长的单条内容会降级为连续纯文本块，以保证分页可靠。
- 标题、公司行和小节标题继续遵循“与下一块同页”的规则。

### 7. 长字符串保护

预览正文增加自动换行和断词保护，避免超长英文、URL 或连续字符横向溢出纸张。

---

## 验证与回归测试

### 固定测试样例

新增 `tests/resume-fixtures.js`，包含：

- 完整有效简历
- 缺少必填字段的简历
- 开始时间晚于结束时间的简历
- 包含超长个人优势、总结和要点的简历

### 已完成验证

- 所有 JSX 文件通过 Babel 编译。
- 正常简历不会产生阻断错误。
- 缺少姓名和公司名称时会产生两个阻断错误。
- 日期倒置时能产生优化建议。
- 超长项目内容能产生精简建议。
- 超长文本能拆成多个不超过约 700 字的分页块。
- 独立单文件中的 `app.jsx`、`editor.jsx`、`preview.jsx` 已与源码同步。

### 尚需手动验证

受浏览器对 `file://` 页面自动控制的安全限制，以下操作需在 Chrome 中手动验证：

- 点击“下载”并确认导出检查弹窗。
- 实际生成和打开 PDF。
- 检查多页 PDF 是否存在视觉裁切。
- 验证 JSON 文件的真实下载与选择导入流程。

---

## 主要文件

- `src/app.jsx`：数据版本、迁移、历史快照、保存状态和质量检查规则。
- `src/editor.jsx`：数据管理、导出检查、保存状态 UI 和 PDF 导出流程。
- `src/preview.jsx`：富文本拆分、自动分页和溢出保护。
- `tests/resume-fixtures.js`：固定回归测试数据。
- `index.html`：唯一运行入口。

## 当前已知限制

- 数据和历史快照仍存储在当前浏览器的 `localStorage` 中。
- 历史快照不会自动跨浏览器或跨电脑同步。
- JSON 备份需要用户主动下载并妥善保存。
- 极端超长内容虽然可以自动分页，但从简历产品效果看仍建议主动精简。
- 项目可以完全离线运行，但仍需要 Chrome 等现代浏览器环境。

## 后续建议

下一阶段可继续处理：

- 增加可自动执行的浏览器和 PDF 回归测试。
- 清理富文本粘贴内容和历史无效样式。
- 补充撤销、恢复及更细粒度的历史版本能力。

---

## 第三阶段：离线运行与工程结构收敛

完成日期：2026-06-08

### 1. CDN 依赖本地化

以下固定版本依赖已保存到 `vendor/`，运行时不再访问 CDN：

- React 18.3.1
- ReactDOM 18.3.1
- Lucide 0.487.0
- html2canvas 1.4.1
- jsPDF 2.5.1

Babel Standalone 7.29.0 也已保存在本地，但只供离线构建脚本使用，不再由浏览器运行时加载。

根目录 `index.html` 增加了内容安全策略，禁止页面发起外部网络连接。断网不会影响编辑、保存、预览和 PDF 生成所需依赖的加载。

### 2. 唯一源码

业务代码统一放在 `src/`：

- `src/app.jsx`
- `src/controls.jsx`
- `src/editor.jsx`
- `src/icons.jsx`
- `src/preview.jsx`
- `src/tweaks-panel.jsx`

`dist/app.js` 是自动生成文件，不作为人工维护源码。修改业务代码后执行：

```bash
node scripts/build.js
```

构建脚本会按固定顺序合并并编译 5 个 JSX 源文件。

### 3. 唯一入口

项目现在只保留一个可运行 HTML：

- `index.html`

已移除：

- `index-standalone.html`
- `AI 简历编辑器.html`

本地使用时，在 Chrome 中打开根目录的 `index.html` 即可。

### 4. 样式整理

页面样式统一放入：

- `assets/app.css`
- `assets/tailwind.css`

HTML 中不再保留大段重复业务样式。

### 5. 本阶段验证

- 离线构建命令执行成功。
- 连续两次构建的 `dist/app.js` 内容完全一致。
- 生成脚本通过 JavaScript 语法检查。
- `index.html` 引用的本地样式、脚本和依赖文件均存在。
- 根目录只剩一个 HTML 入口。
- 正式运行文件中不存在 CDN 或第三方网络依赖地址。

浏览器自动控制工具因安全策略拒绝访问 `file://` 页面，因此本阶段未通过该工具完成界面点击验证。该限制不影响用户直接使用 Chrome 打开本地文件。

---

## 第四阶段：主题功能与 UI 视觉升级

完成日期：2026-06-08

### 1. 产品化主题入口

编辑器顶部工具栏新增“主题”按钮。点击后打开主题画廊，不再使用开发工具式悬浮面板。

主题画廊提供缩略界面预览、主题说明和当前选中状态，支持直接点击实时切换。

### 2. 内置主题

保留并产品化以下 5 套主题：

- 青绿：沉稳专业的默认深色主题。
- 靛蓝：冷静克制的 B 端工具风格。
- 紫罗兰：更偏 AI 产品的现代视觉。
- 暖橙：温暖、有活力的深色主题。
- 石墨浅色：适合白天和高对比阅读的浅色主题。

### 3. 主题持久化

当前主题保存到浏览器 `localStorage`。刷新或重新打开编辑器后，会自动恢复上次选择。

主题仅影响编辑器界面，不改变简历预览和 PDF 的白底黑字排版。

### 4. UI 视觉升级

新增和统一以下视觉表现：

- 页面背景渐变和轻量网格氛围。
- 编辑区半透明背景与模糊效果。
- 卡片圆角、阴影、悬停反馈和主题化边框。
- 顶部工具按钮统一样式。
- 下载主按钮增加主题色阴影和按压反馈。
- 主题画廊增加界面缩略图和选中状态。
- 输入控件和按钮增加键盘焦点提示。

### 5. 工程清理

- 删除不再使用的 `src/tweaks-panel.jsx`。
- 构建源文件由 6 个精简为 5 个。
- 旧主题面板及其宿主通信代码不再进入 `dist/app.js`。
- 生成脚本体积由约 125 KB 降至约 101 KB。

### 6. 本阶段验证

- JSX 离线构建成功。
- 生成脚本通过 JavaScript 语法检查。
- 连续构建结果一致。
- 5 套主题配置均保留。
- 主题本地存储读写逻辑已接入。
- 旧主题面板代码已从源码和生成文件中移除。
- 正式运行文件仍不存在 CDN 或第三方网络依赖。

受浏览器工具对 `file://` 的访问限制，本阶段的主题画廊点击和视觉效果需要在 Chrome 中刷新 `index.html` 后查看。

---

## 第五阶段：工作区侧边栏与 1:1 布局

完成日期：2026-06-08

### 1. 编辑区与预览区等分

简历编辑页面改为 1:1 双栏布局：

- 左侧为简历配置和编辑区域。
- 右侧为简历实时预览区域。
- 两个区域在扣除侧边栏宽度后，各占剩余工作区的 50%。

已移除原有的 `58%`、最小 `560px` 和最大 `960px` 等硬编码限制。在宽屏设备上，简历配置区域可以充分利用可用空间。

### 2. 左侧工作区导航

页面左侧新增固定侧边栏，包含：

- 简历编辑
- 简历模板
- 模拟面试
- AI 导师
- 职位

当前栏目使用主题强调色显示，鼠标悬停时提供视觉反馈。

### 3. 页面切换

“简历编辑”继续展示现有编辑器和预览功能。

其余栏目已建立独立预留页面，点击侧边栏即可切换，并提供“返回简历编辑”入口。后续功能可直接接入对应工作区，无需再次调整主导航结构。

### 4. 打印兼容

打印和 PDF 模式下会隐藏侧边栏与编辑区域，并将预览区恢复为完整宽度，避免新的三段式布局影响导出结果。

### 5. 本阶段验证

- 5 个导航项均已进入构建产物。
- 编辑区和预览区使用两个相同的 `1fr` 网格列。
- 旧宽度限制已从源码移除。
- 打印模式布局复位规则已加入。
- 离线构建及 JavaScript 语法检查通过。
- 连续构建结果一致。
- 正式运行文件仍不包含外部网络依赖。

---

## 第六阶段：默认主题、弹窗定位与 PDF 命名

完成日期：2026-06-08

### 1. 默认主题调整

- 默认界面主题由“青绿”调整为“石墨浅色”。
- 未选择过主题的用户会直接使用石墨浅色。
- 使用旧版默认青绿主题的本地数据会迁移到石墨浅色。
- 用户后续主动选择的其他主题仍会被保留。

### 2. 主题弹窗定位修复

主题弹窗改为通过 React Portal 渲染到 `document.body`。

修复了编辑区使用模糊背景后，浏览器将 `fixed` 弹窗错误地相对局部容器定位，导致弹窗随滚动位置下移的问题。弹窗现在始终相对浏览器视口居中。

### 3. PDF 文件名规则

PDF 文件名不再使用顶部简历项目名称，改为：

```text
姓名-意向岗位.pdf
```

规则：

- 姓名为空时使用“李四”。
- 意向岗位为空时使用“产品经理”。
- 两项均为空时生成 `李四-产品经理.pdf`。
- 文件名中的系统非法字符会自动替换为下划线。

### 4. 本阶段验证

- 默认主题和旧主题迁移规则已进入构建产物。
- 主题弹窗已确认通过 Portal 渲染。
- PDF 文件名完整输入、全部缺失、部分缺失和非法字符测试均通过。
- 离线构建、JavaScript 语法检查和连续构建一致性检查通过。

---

## 方案 d：模块标题图标化 · 统一卡片结构（规划中）

拟定日期：2026-06-08

这是编辑器版式 A / B / C 之外的第四套设计方向，聚焦模块视觉与内容层次。

### 1. 个人信息保持现状

个人信息模块不做强化调整，继续使用现有 SectionCard + 表单布局。

### 2. 模块标题统一：标题 + 图标 + 填写卡片

所有编辑模块（个人优势、工作经历、教育经历）的结构统一为三层：

- 模块标题（附加对应图标前缀）。
- 内容填写卡片。

主要变化：

- 当前“工作经历”和“教育经历”的纯文本标题行改为带图标的 SectionCard 式标题区。
- 工作经历改为一个整卡片，内部按公司分组展示多个项目。
- 每家公司及其所含项目均为可折叠子模块（点击标题行折叠/展开）。
- 教育经历中的多段经历（如本科 + 硕士）同样支持折叠。

### 3. 模块标题字号放大

所有模块标题区由当前 text-[15px] 调整为 text-[17px]（放大 2 号）。

---

## v3.0：职位匹配分析

发布日期：2026-06-09

### 1. 职位推荐工作区

- 左侧导航新增“职位推荐”。
- 支持职位列表、搜索、业务方向筛选和排序。
- 支持查看岗位职责、任职要求和求职进度。
- 职位数据使用统一字段协议，可继续替换为飞书同步数据。

### 2. 多模型配置

- 新增 Provider、Model、API Key 和 Base URL 配置界面。
- 支持 OpenAI、DeepSeek、Kimi、豆包、Claude及自定义 OpenAI 兼容接口。
- 模型名称支持手动输入，不依赖应用内置列表更新。
- 支持在设置中测试模型连接。

### 3. 工作经历脱敏

匹配分析不会上传完整简历，只发送：

- 公司名称与任职时间。
- 项目名称、角色和项目时间。
- 项目总结、重点项目和荣誉。

以下内容不会发送给模型：

- 姓名、手机号、邮箱和头像。
- 求职方向、个人关键词和个人优势。
- 学校、学历、专业和教育时间。

### 4. 基础分析协议

- 总匹配度和各雷达维度统一使用 `0–100` 分。
- 固定七个维度：核心职责、技能关键词、项目与成果、年限与职级、行业与业务、基础条件、地区与方向。
- 模型返回结果必须通过字段、维度数量、维度顺序和分数范围校验。
- 校验失败或 API 请求失败时展示明确错误，不再使用模拟分数兜底。

### 5. 雷达图与分析结论

- 使用原生 SVG 展示七维雷达图。
- 展示总匹配度、整体结论、匹配优势、关键缺口和投递建议。
- 职位列表和详情页同步展示最近一次匹配得分。

### 6. 结果缓存与过期判断

- 每个职位保存最近一次分析结果。
- 缓存记录职位版本、工作经历版本、模型配置和分析协议版本。
- 页面刷新后可直接恢复结果，无需重复消耗模型 API。
- 工作经历、岗位、模型或协议变化后，旧结果保留但标记为过期。
- 用户点击“重新分析”后使用最新输入生成并覆盖缓存。

### 7. v3.0 验证

- 离线构建成功。
- 工作经历序列化已确认不包含个人信息和教育经历。
- 真实模型 API 调用成功。
- 模型返回通过七维结构校验。
- 雷达图、优势、缺口和建议正常渲染。
- 刷新页面后缓存结果正常恢复。

### 8. v3.0 已知限制

- 模型 API Key 保存在当前浏览器 `localStorage`。
- 职位数据暂未实现实时飞书服务端同步。
- 简历改写仍为模拟结果。
- 底部追问输入框尚未接入真实模型对话。
- 不支持流式输出和 Markdown 渲染。

---

## v3.1：自动明暗主题

发布日期：2026-06-09

### 1. 本地时间自动切换

- 新增“自动”主题模式，并作为默认模式。
- 使用浏览器本地时间判断当前明暗模式。
- `07:00–18:59` 自动使用浅色，`19:00–06:59` 自动使用深色。
- 页面保持打开时每分钟检查一次时间；窗口重新获得焦点时立即检查。
- 也可以手动固定为浅色或深色，不受时间切换影响。

### 2. 三组深浅配色

- 深色模式提供宝蓝、青绿、紫罗兰 3 套配色。
- 浅色模式提供宝蓝、青绿、紫罗兰 3 套配色。
- 原“石墨 · 默认浅色”调整为“宝蓝 · 浅色”。
- 移除靛蓝、暖橙两套独立深色主题，使深浅配色一一对应。

### 3. 主题偏好保存

- 分别保存用户选择的配色和模式。
- 自动模式切换明暗时保留当前配色。
- 兼容旧主题缓存；无法映射的旧配色回退为宝蓝。
- 主题仍只影响应用界面，不影响简历预览和 PDF 白底排版。

### 4. v3.1 验证

- 本地白天时间首次打开时自动显示宝蓝浅色。
- 主题变量、页面背景和组件颜色能够同步更新。
- 源码构建和差异格式检查通过。

---

## v3.5：岗位追问对话

发布日期：2026-06-09

### 1. 连续追问

- 职位助手新增“继续追问”入口。
- 用户可以围绕当前职位、工作经历和匹配结果继续提问。
- 支持 Enter 发送和 Shift + Enter 换行。
- 单次输入限制为 1000 字。
- 模型生成期间禁用重复发送。

### 2. 对话上下文

每次模型请求包含：

- 当前职位 JD。
- 仅包含工作经历的脱敏简历数据。
- 当前职位最近一次匹配分析。
- 当前职位最近 20 条问答消息。
- 用户本次问题。

不同职位的对话相互隔离，不共享上下文。

### 3. 消息气泡

- 用户消息使用右侧强调色气泡。
- 模型回答使用左侧普通气泡。
- 生成期间显示“正在思考”状态。
- 模型回答按纯文本展示并保留换行。
- 自动清理加粗标记、标题符号、代码标记和 Markdown 分隔线。
- 初始状态提供四个推荐问题。

### 4. 基础会话管理

- 对话按职位 ID 保存到浏览器 `localStorage`。
- 页面刷新后可恢复当前职位的历史消息。
- 支持清空当前职位对话。
- 请求失败时保留用户问题并显示错误和重试按钮。
- 最多携带最近 20 条消息，避免上下文持续膨胀。

### 5. 上下文过期

对话缓存记录：

- 工作经历版本。
- 职位版本。
- 模型配置。
- 对话协议版本。

任一内容变化后，旧消息仍可查看并显示过期提示。用户再次发送时自动使用最新上下文开始新对话。

### 6. 事实与隐私约束

- 对话继续只上传工作经历，不发送个人信息、个人优势和学历。
- 系统提示要求模型不得把输入中不存在的内容描述为候选人事实。
- 未体现的能力应描述为“缺少证据”，不得直接判断候选人不具备。
- 页面明确提示模型内容仅供参考，使用前需要核对事实。

### 7. 明确不做

- 不做流式输出。
- 不做 Markdown 渲染。
- 不做多会话列表、会话命名或分支对话。
- 不支持编辑或删除单条消息。
- 不提供跨职位对话共享。

### 8. v3.5 验证

- 真实模型 API 首轮追问成功。
- 第二轮问题能够结合前序消息继续回答。
- 用户与模型消息气泡正常展示。
- 模型回答能够转为纯文本显示。
- 刷新页面后消息缓存正常恢复。
- 对话协议变化后旧消息会标记过期，并在再次发送时自动开启新上下文。

### 9. v3.5 已知限制

- 自由文本模型仍可能混合职位要求和候选人事实，回答使用前必须人工核对。
- 对话历史仅保存在当前浏览器。
- 不对模型回答执行逐句证据引用和事实 Schema 校验。

---

## v4.0：面试闪卡

发布日期：2026-06-09

### 1. 导航与工作区

- 删除“简历模板”入口。
- 将原“模拟面试”调整为“面试闪卡”，位于第三个工作区入口。
- 新增独立的桌面三栏训练工作区。

### 2. 规定字段题库

- 支持通过 JSON 文件导入外部题库。
- 校验题库版本、必填字段、字段类型以及项目、题目和追问 ID。
- 校验失败时不覆盖当前题库，并展示具体字段路径。
- 项目提供 `interview-library.example.json` 作为字段协议示例。
- 首次运行内置基础示例题库，确保训练流程可以直接体验。

### 3. 闪卡训练

- 按项目展示题目目录、已训练数量和高危数量。
- 展示题干、题号、分类与标签，长内容滚动时题干吸顶。
- 提供 90 秒倒计时，支持开始、暂停和重置。
- 结构化展示考察意图、问题类型、回答框架、完整回答和风险点。
- 默认仅展开考察意图，其余内容按需查看。
- 展示默答、框架、完整回答、追问、标记五步训练阶段。

### 4. 追问与状态

- 支持从主问题进入连续追问训练。
- 支持上一条、下一条追问以及返回主问题。
- 题目支持标记为已掌握、待强化和高危。
- 状态变更后实时更新项目进度与统计。

### 5. 进度和统计

- 自动保存当前项目、当前题目及每个项目最后训练位置。
- 页面刷新后重新进入模块可恢复训练位置和题目状态。
- 记录题目训练次数、最近训练时间和最近训练状态。
- 展示本次时长、今日累计时长、今日训练数、最近 12 题表现和项目掌握度。
- 支持重置全部训练进度，题库数据保持不变。

### 6. v4.0 明确不做

- 不根据简历或模型动态生成题库。
- 不做 AI 实时评分、录音识别、摄像头或语音能力。
- 不做键盘快捷键和小屏适配。
- 不做云端同步、题库缓存过期检测或 V1/V2 版本切换。
